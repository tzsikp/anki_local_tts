from __future__ import annotations

# Preset CRUD UI: provider dropdown, provider options form built from
# Provider.options_schema(), cleanup options, regex rules table with preview,
# Test Synthesis button. Stub.
